suggestions_model = {
    'items': [
        {
            'severity': str,
            'violationMessage': str,
            'suggestion': str,
            'affectedAssetsCount': int,
        }
    ],
    'totalCount': int
}
